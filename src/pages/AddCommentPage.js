import AddNewComment from "../components/AddComment/AddNewComment";
import Layout from "../layout/layout";

const AddComment = () => {
    return (  
        <Layout>
            <AddNewComment/>
        </Layout>
    );
}
 
export default AddComment;